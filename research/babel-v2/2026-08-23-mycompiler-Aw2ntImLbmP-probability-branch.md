# Public Lucky Jet artifact: myCompiler Aw2ntImLbmP

Source: https://www.mycompiler.io/view/Aw2ntImLbmP

## Verified facts
- Public Python artifact titled `Lucky jet predictor`, author shown as `akhenaton`.
- Contains a separate simulated multiplier generator:
  - 70% branch: 1.0–2.0
  - next 20%: 2.0–5.0
  - top 10%: 5.0–15.0
- Also contains the older Lucky Jet `Grosse Côte` template:
  - future time offset: +1 minute
  - target bounds: `hack=12.25`, `max=20.62`
  - target selected with `random.uniform(hack, max)`
  - `Assurance = target / 2`
  - `Fiable = Assurance / 2`
- The apparent analysis gate is not meaningful (`heureHour == 24` cannot be true for normal `datetime.hour`).

## Classification
This is a public comparative lineage artifact only. It is **not** verified as BABEL/AllPredictor source code, and its prediction path is random/simulated rather than based on live `/history` data.

## Why it matters
It shows another public branch where the old Grosse Côte signal template was combined with an explicit, hand-authored probability distribution for simulated Lucky Jet multipliers. It should not be used as the original BABEL V2 trigger or confidence logic.

No credentials or secret-looking strings from the source were preserved here.