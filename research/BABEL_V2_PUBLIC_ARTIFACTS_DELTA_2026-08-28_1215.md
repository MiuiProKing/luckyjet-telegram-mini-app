# BABEL V2 public artifacts delta — 2026-08-28 12:15 Europe/Kyiv

## New verified provenance node: @lastarsey / @couponfiablexbet

### Confirmed

1. AllPredictor's current public Terms page identifies `@lastarsey` as its Telegram contact and repeats the same handle for payment disputes/support.
   - https://allpredictor.com/terms

2. AllPredictor's current public Support page also lists `@lastarsey` as the Telegram support contact.
   - https://allpredictor.com/support

3. A currently indexed public Telegram channel titled `BABEL SEY` uses the exact username `@couponfiablexbet` and its public description explicitly says `ADMIN —> @lastarsey`.
   - https://tg.me/couponfiablexbet
   - https://telemetr.io/en/channels/1805151250-couponfiablexbet

4. The same indexed channel description contains the public BABEL SEY branding and is thematically tagged around `prédiction`, `bbsy`, `mise`, and `allpredictor.com` by public Telegram analytics mirrors.

### Inference / not proven

- This creates a stronger public provenance bridge between the current AllPredictor support identity (`@lastarsey`) and a BABEL SEY-branded prediction channel (`@couponfiablexbet`).
- It does **not** prove that `@lastarsey` authored the original BABEL Predictor V2 algorithm, nor does it reveal any predictor source code, API key, private backend, or hidden endpoint.
- No new original `predict/analyse/detection` function, LuckyJet history-window formula, weights, thresholds, or `/api/v1/luckyjet/*` implementation was recovered in this run.

## Safety

Only publicly indexed pages were inspected. No authentication bypass, private repository access, hidden-directory enumeration, exploitative scanning, or credential recovery was performed. No credentials or secrets are stored in this manifest.
