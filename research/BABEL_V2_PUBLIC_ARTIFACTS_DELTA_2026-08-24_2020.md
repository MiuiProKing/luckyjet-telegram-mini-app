# BABEL V2 public artifact delta — 2026-08-24 20:20 Europe/Kyiv

## Confirmed new public artifact

Source: https://tlmtr.io/ru/channels/1993921907-babelsey225

The public archive for `@babelsey225` currently exposes a compact sequence associated with the post `NOUVEAU ROBOT DE PRÉDICTION 1WIN V2`:

- `✅ NOUVEAU SIGNAL PETIT` — target `3.0x`, confidence `72%`, time `22:44`.
- `✅ NOUVEAU SIGNAL PETIT` — targets `2x 4x`, confidence `60%`, time `22:38`.
- Additional adjacent public entries show repeated dual-target PETIT outputs: `22h31 2x 4x`, `22h27 2x 3x`, `22h23 2x 4x`, `22h19 2x 3x`, `21h12 2x 3x`.
- Public validation comments next to the sequence include `Côte 4 validé` and `Validé`.

## Why this matters

This is a new structural clue for PETIT mode. Earlier captured PETIT examples could look like a single target (`1.88X`, etc.). The newly indexed sequence shows that PETIT can also emit **two discrete target levels** such as `2x + 3x` or `2x + 4x`, while a separate PETIT signal can emit a single `3.0x` target.

### Confirmed

- PETIT output is not restricted to one target multiplier.
- In this public V2-adjacent sequence, `2x` acts as a lower target while `3x`/`4x` appears as a higher second target.
- Confidence is published alongside at least some of these PETIT signals (`60%`, `72%`).

### Inference only

The dual-target format is consistent with a two-level output layer (for example, base target + higher target/insurance), but no public source in this run exposes the original function, threshold table, or formula that selects `2x/3x/4x`. Do not treat this as proof of the internal BABEL algorithm.

## Security / scope

No private endpoints, credentials, tokens, session identifiers, or access-controlled resources were used or copied. Only publicly indexed content was recorded.
