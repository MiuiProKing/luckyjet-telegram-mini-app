# BABEL V2 public artifact delta — 2026-08-28 07:11 Europe/Kyiv

## New verified AllPredictor sites-manager/security-layer detail

Public source: https://allpredictor.com/

### Confirmed

The current public AllPredictor homepage documents a **Security** section in the dashboard for hosted sites. It states that users can:

- protect hosted sites with a password;
- create masked/obfuscated links;
- obfuscate source code.

The same page also confirms the hosted-site route model `name.allpredictor.com`, plus dashboard management for logs, API keys, Workers KV, and custom domains.

This is relevant to the BABEL/Lucky Jet V2 lineage because AllPredictor's public sites-manager supports a protection/obfuscation layer around hosted site surfaces. It provides a benign explanation for why some historically referenced hosted slugs or front-end assets may no longer expose readable source directly.

### Important boundary

This artifact **does not** establish that the historical Lucky Jet/BABEL predictor used password protection, masked links, or code obfuscation. That remains unproven.

No authentication was bypassed, no protected route was accessed, no hidden-directory enumeration was performed, and no credentials/tokens/session secrets were collected.

### Predictor-core status

No genuinely new public source for the original BABEL Predictor V2 `predict/analyse/detection` function, history-window size, weights, thresholds, target-selection formula, timing rules, `/api/v1/luckyjet/predict`, `/api/v1/luckyjet/market`, `/coefficients`, or `/app/servers.js` was verified in this pass.
