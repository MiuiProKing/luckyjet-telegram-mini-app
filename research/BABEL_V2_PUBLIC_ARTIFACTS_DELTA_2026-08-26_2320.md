# BABEL V2 public artifacts delta — 2026-08-26 23:20 Europe/Kyiv

## New verified artifact: confirmation flow moved into the app

Public BABEL OFFICIEL archive snapshot for `@babelseyy` contains a fresh post stating:

> `TOUT PASSE DANS L’APPLICATION MAINTENANT ... LE PROBLÈME DE CONFIRMATION AVEC LE BOT EST RÉGLÉ`

The same fresh sequence also tells Android users to uninstall the old build and install the new update. Nearby posts in the same archive include `NOUVEAU ROBOT DE PRÉDICTION 1WIN V2`, the Lucky Jet strategy menu, and BBSY/AllPredictor material.

Public source:
- https://tlmtr.io/ru/channels/1963411992-babelseyy
- corroborating snapshot: https://tlmtr.io/uk/channels/1963411992-babelseyy

## Confirmed facts

- A fresh public BABEL OFFICIEL post states that the confirmation flow now happens inside the application and that the prior bot-confirmation problem is fixed.
- In the same update sequence, Android users are instructed to remove the old version and install the new one.
- The archive places this update adjacent to the current `NOUVEAU ROBOT DE PRÉDICTION 1WIN V2` / Lucky Jet strategy content.

## Inference (not confirmed)

This is evidence of an architecture/UI-flow change: some confirmation/validation step that previously depended on a Telegram bot may have been migrated into the AllPredictor/mobile-app layer. It does **not** prove that prediction calculation itself moved into the app, nor does it reveal the original BABEL Predictor V2 formula, API route, history window, weights, thresholds, or target-selection code.

## Security / scope note

Only publicly indexed material was inspected. No authentication bypass, hidden-path enumeration, access-control bypass, credential recovery, or secret extraction was performed. No tokens, passwords, session IDs, API keys, or other credential-like values are preserved here.
