# BABEL V2 Public Artifacts Delta — 2026-08-25 04:50 Europe/Kyiv

## New verified public artifact

Source: https://telemetr.io/en/channels/1824595312-babelseypro235
Channel/archive label: BONUS CASINO / @babelseypro235

### Confirmed observations

The public archive contains a distinct `LUCKY JET PRO 4` signal format using a target range, an assurance range, and a short time window. Verified examples include:

- `CÔTE: 50–100X` / `ASSURANCE: 10–20X` / `19:59–20:01`
- `CÔTE: 50–70X` / `ASSURANCE: 10–20X` / `19:52–19:54`
- `CÔTE: 50–70X` / `ASSURANCE: 10–20X` / `19:40–19:43`
- `CÔTE: 50–70X` / `ASSURANCE: 10–20X` / `19:27–19:30`

A second indexed archive view for the same public channel also exposes lower-tier PRO 4 examples:

- `CÔTE: 20X` / `ASSURANCE: 4X` / `20:10–20:11`
- `CÔTE: 20X` / `ASSURANCE: 4X` / `20:00–20:01`
- `CÔTE: 15X` / `ASSURANCE: 3X` / `19:45–19:46`
- `CÔTE: 15X` / `ASSURANCE: 3X` / `19:34–19:35`

The same archive also publicly states that the newer Telegram bots are “avec API connectés” and associates the BBSY bot family with `@allpredictorv4bot` and other previously known bot names.

### Interpretation

Confirmed: PRO 4 is publicly represented as a tiered target/assurance system, not a single fixed multiplier. At least two bands are visible in the archive: a lower band around 15–20X with 3–4X assurance, and a higher band around 50–100X with 10–20X assurance. Signal windows range from about 1 to 3 minutes in the verified examples.

Inference only: these bands may correspond to different confidence/market-state tiers or separate PRO4 modes. No original source function, formula, threshold table, history-window size, or deterministic selection rule is exposed in the archive, so the internal prediction logic remains unverified.

### Safety / provenance note

Only publicly indexed archive text was recorded. No authentication bypass, private content, hidden paths, tokens, passwords, session secrets, or credential material were accessed or preserved.
