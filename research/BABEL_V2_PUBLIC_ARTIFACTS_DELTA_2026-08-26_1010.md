# BABEL V2 Public Artifacts Delta — 2026-08-26 10:10 (Europe/Kyiv)

## New verified public artifact

Source: public Telemetr archive for `@babelseypro235` / `onepiece_vostfr`.

A public post tied to the same BBSY bot/update cluster that includes `@allpredictorv4bot` states that future predictions would be "automatically linked" to promo code `BBSY`, and claims that users not registered through that code would no longer receive/pass the predictions.

Public source:
- https://telemetr.io/en/channels/1824595312-onepiece_vostfr

Relevant neighboring context in the same archive:
- `@allpredictorv4bot` appears in the bot update list.
- The author says all Telegram bots were updated.
- A Play Store update is mentioned in the same sequence.
- Separate posts describe the new bots as using connected APIs.

## Confirmed facts

- The statement was publicly posted in the BBSY/BABEL-associated archive.
- It is presented as an operational access/gating rule linked to promo code `BBSY`.
- It appears in the same public update stream as `@allpredictorv4bot` and the broader bot fleet.

## Inference / not proven

- This does **not** prove a technical implementation such as server-side affiliate-ID validation, account binding, API-key gating, or WebApp entitlement checks.
- It may be marketing language or an operational policy rather than predictor-core logic.
- No new original `predict/analyse/detection` function, Lucky Jet formula, history window, weights, thresholds, or exact `/api/v1/luckyjet/*` implementation was recovered in this pass.

## Safety

No credentials, tokens, session IDs, private endpoints, authentication bypasses, hidden paths, or non-public secrets were collected or reproduced.
